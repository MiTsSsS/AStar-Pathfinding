using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Swordsman : BaseUnit
{
    // Start is called before the first frame update
    void Start()
    {
        initializeAttackRange(1, -1, -1, 1);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}